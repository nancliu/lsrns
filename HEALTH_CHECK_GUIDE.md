# 健康检查按钮说明

## 功能说明

**健康检查按钮**位于场景浏览器页面右上角，用于**验证后端 API 服务的状态**。

### 按钮位置
```
┌────────────────────────────────────────────────┐
│ 🎬 仿真场景库浏览器    [刷新数据] [健康检查] ← │
└────────────────────────────────────────────────┘
```

---

## 用途

### 1️⃣ 验证后端服务是否正常运行
- 检查 API 服务是否启动
- 验证路由是否正确注册
- 确认数据库连接状态

### 2️⃣ 诊断连接问题
点击健康检查按钮会显示：
- ✅ **后端状态**: API 是否在线
- ✅ **场景数量**: scenario_index.json 是否可访问
- ✅ **API 地址**: 数据文件的完整路径

### 3️⃣ 三级降级机制
健康检查实现了智能降级处理：

```
级别1: 场景服务健康检查
   └─> GET /api/v1/scenario/health
       返回: { status, scenario_count, index_path }

级别2: 全局API健康检查 (如果级别1失败)
   └─> GET /api/v1/health
       返回: { status, scope }

级别3: 本地数据状态 (如果级别1/2都失败)
   └─> 显示已缓存的场景数量
       返回: "✓ 场景数据已加载: 449 个"
```

---

## 点击结果示例

### ✅ 正常情况
```
后端健康检查 ✅

场景服务
状态: healthy
场景数: 449

API 地址: output/scenarios/scenario_index.json
```

### ⚠️ 场景服务不可用，但API正常
```
后端健康检查 ✅

API 状态: healthy

场景数据: 449 (已缓存)
```

### 🔄 后端暂时不可用（数据已加载）
```
后端连接状态

✓ 场景数据已加载: 449 个
✓ 应用正常运行

错误: Failed to fetch
```

---

## API 端点详情

### 场景服务健康检查
```
GET /api/v1/scenario/health

响应 (200 OK):
{
  "status": "healthy",
  "scenario_count": 449,
  "index_path": "output/scenarios/scenario_index.json"
}
```

### 全局API健康检查
```
GET /api/v1/health

响应 (200 OK):
{
  "status": "healthy",
  "scope": "api"
}
```

---

## 常见问题

### Q: 健康检查显示 404 错误？
**A**:
- 确保 API 服务已启动: `.\start_api.ps1`
- 检查服务是否监听在 `http://localhost:8000`
- 查看终端是否有错误信息

### Q: 为什么要检查三个不同的端点？
**A**:
- 提高可靠性：一个端点失败不会影响整体体验
- 诊断不同层级的问题：区分服务级别 vs API级别 vs 数据级别
- 优雅降级：即使后端不可用，已加载的数据仍可用

### Q: 健康检查对性能有影响吗？
**A**:
- 完全异步调用，不阻塞UI
- 点击后立即返回结果
- 无需刷新页面

### Q: 场景数据显示"已缓存"是什么意思？
**A**:
- 表示场景数据已在页面加载时成功获取
- 来自 `/output/scenarios/scenario_index.json`
- 即使后端暂时不可用，这些数据仍可正常使用
- 刷新页面会重新加载最新数据

---

## 故障排查流程

如果页面功能异常，按以下步骤排查：

```
1. 点击 [健康检查] 按钮
   ↓
   成功 ✅ → 后端正常，可能是页面逻辑问题
   失败 ❌ → 检查步骤2

2. 检查 API 服务状态
   - 打开终端，查看是否有错误日志
   - 确认 http://localhost:8000 是否可访问

3. 检查网络连接
   - 浏览器开发者工具 (F12) → Network 标签
   - 查看请求是否发送，响应是否正确

4. 刷新页面
   - 清除缓存: Ctrl+Shift+Delete
   - 重新加载: Ctrl+Shift+R

5. 重启 API 服务
   - 终止现有服务
   - 运行: .\start_api.ps1
```

---

## 总结

| 功能 | 说明 |
|------|------|
| **目的** | 验证后端 API 服务状态 |
| **位置** | 页面右上角 |
| **调用** | 异步非阻塞 |
| **降级** | 三级智能降级处理 |
| **用途** | 诊断连接/服务问题 |
| **影响** | 无性能开销 |

---

**健康检查是一个诊断工具，帮助您快速了解后端服务状态。** ✨

Created: 2025-11-11
