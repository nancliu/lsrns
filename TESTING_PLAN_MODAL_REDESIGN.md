# 场景浏览器模态框重设计 - 测试计划

**日期**: 2025-11-14
**版本**: 1.0
**目标**: 全面验证模态框重设计功能

---

## 🎯 测试目标

验证以下功能在实际浏览器环境中正常工作：

1. ✅ 场景详情模态框的显示和数据加载
2. ✅ event_description.json 的异步加载
3. ✅ 案例创建模态框的简化工作流
4. ✅ 自动回退机制
5. ✅ 按钮交互和工作流

---

## 📋 测试前准备

### 1. 环境准备

```bash
# 1. 启动后端API
cd d:\projects\OD_SIM
.\start_api.ps1

# 输出应该显示:
# ✓ API运行在 http://localhost:8000
# ✓ Swagger文档 http://localhost:8000/docs
```

### 2. 打开前端

```
http://localhost:8000/frontend/scenarios/scenario_browser.html
```

应该看到：
- ✅ 顶栏显示 "🎬 仿真推演场景集"
- ✅ 左侧导航栏
- ✅ 场景统计信息（总场景数、事件类型、管控策略、已创建案例）
- ✅ 筛选区域
- ✅ 场景表格

### 3. 打开浏览器开发者工具

按 `F12` 打开开发者工具，准备观察：
- **Network 标签**: 观察网络请求（特别是 event_description.json）
- **Console 标签**: 观察日志和错误信息
- **Elements 标签**: 检查 DOM 结构

---

## 🧪 功能测试用例

### 测试集合 #1: 场景详情模态框

#### TC-1.1: 打开场景详情模态框

**步骤**:
1. 在场景表格中找到任意场景行
2. 点击左边的"详情"按钮
3. 观察模态框显示

**预期结果**:
- ✅ 模态框立即打开（< 100ms）
- ✅ 基础信息立即显示（场景ID、事件ID、事件类型、管控策略）
- ✅ 标题显示 "📍 场景详情"
- ✅ 右上角有关闭按钮 ×
- ✅ 底部有"关闭"按钮

**验证**:
- [ ] 模态框显示无延迟
- [ ] 所有基础字段预填
- [ ] 模态框在屏幕中央

---

#### TC-1.2: 事件描述加载

**步骤**:
1. 打开浏览器 Developer Tools → Network 标签
2. 打开任意场景的详情模态框
3. 观察网络请求

**预期结果**:
- ✅ 应该看到一个 `/output/scenarios/{event_type}/{scenario_id}/event_description.json` 请求
- ✅ 状态码为 200（文件存在）或 404 自动回退
- ✅ "事件描述" 字段应该填充完整内容

**验证示例 (交通事故场景)**:
```
Request URL: http://localhost:8000/output/scenarios/01_accident/scenario_10754_no_control/event_description.json
Status: 200 OK
Response: JSON with event_description, location, time, impact fields
```

**验证**:
- [ ] Network 标签中找到 event_description.json 请求
- [ ] 状态码为 200
- [ ] 响应包含完整的 JSON 数据
- [ ] "事件描述"字段显示完整的事件信息

---

#### TC-1.3: 位置信息填充

**步骤**:
1. 打开详情模态框
2. 滚动到"事件发生地点与时间"部分
3. 观察以下字段：
   - 道路
   - 方向
   - 里程（KM）
   - 路网Edge ID
   - 交叉口ID

**预期结果**:
- ✅ 所有5个字段都填充了值
- ✅ 值来自 event_description.json 的 location 部分
- ✅ 如果文件不存在，应该显示"未知"或使用备选数据

**示例数据** (scenario_10754_no_control):
```
道路: G5京昆高速（成雅段）
方向: 下行
里程: K1834.3+000
路网Edge ID: -3734
交叉口ID: -55409
```

**验证**:
- [ ] 道路字段有值（不是"未知"）
- [ ] 方向字段有值
- [ ] 里程字段有值
- [ ] Edge ID 字段有值
- [ ] Junction ID 字段有值

---

#### TC-1.4: 时间信息填充

**步骤**:
1. 打开详情模态框
2. 滚动到"时间信息"部分
3. 观察以下字段：
   - 开始时间
   - 结束时间
   - 持续时长

**预期结果**:
- ✅ 所有3个字段都填充了时间值
- ✅ 格式为 YYYY-MM-DD HH:MM:SS
- ✅ 持续时长显示为"XXXh"格式（例如 0.52h）

**示例数据** (scenario_10754_no_control):
```
开始时间: 2025-06-10 10:43:48
结束时间: 2025-06-10 11:14:50
持续时长: 0.52h
```

**验证**:
- [ ] 开始时间显示正确的日期和时间
- [ ] 结束时间显示正确的日期和时间
- [ ] 持续时长以"h"结尾

---

#### TC-1.5: 事件影响填充

**步骤**:
1. 打开详情模态框
2. 滚动到"事件影响"部分
3. 观察"影响描述"文本区域

**预期结果**:
- ✅ 显示具体的影响信息
- ✅ 格式为: "影响车道: 应急车道\n车道ID: -3734_0"
- ✅ 如果没有影响数据，显示"暂无详细影响信息"

**示例数据** (scenario_10754_no_control):
```
影响车道: 应急车道
车道ID: -3734_0
```

**验证**:
- [ ] 影响信息显示在文本区域
- [ ] 包含影响车道名称
- [ ] 包含车道ID

---

#### TC-1.6: 关闭详情模态框

**步骤**:
1. 打开详情模态框
2. 点击以下任意一个关闭方式：
   - 右上角的 × 按钮
   - 底部的"关闭"按钮
3. 观察模态框

**预期结果**:
- ✅ 模态框立即关闭
- ✅ 返回到场景表格
- ✅ 表格内容保持不变
- ✅ 可继续进行其他操作

**验证**:
- [ ] 点击 × 按钮后模态框关闭
- [ ] 点击"关闭"按钮后模态框关闭
- [ ] 场景表格正常显示

---

### 测试集合 #2: 创建案例模态框

#### TC-2.1: 打开创建案例模态框

**步骤**:
1. 在场景表格中找到任意场景行
2. 点击右边的"创建"按钮
3. 观察模态框显示

**预期结果**:
- ✅ 模态框立即打开
- ✅ 显示标题"创建仿真案例"
- ✅ 场景信息（只读）已预填
- ✅ EdgeData 和 TripInfo 复选框显示，默认勾选
- ✅ 有"✓ 创建"和"取消"按钮

**验证**:
- [ ] 模态框显示无延迟
- [ ] 模态框在屏幕中央
- [ ] 模态框宽度约600px

---

#### TC-2.2: 预填信息验证

**步骤**:
1. 打开任意场景的创建模态框
2. 观察以下字段（都应该是只读的）：
   - 场景ID
   - 事件类型
   - 管控策略
   - 模拟时间段

**预期结果**:
- ✅ 所有字段都填充了当前场景的信息
- ✅ 所有字段都是只读的（背景色为灰色 #f5f5f5）
- ✅ 用户无法编辑这些字段

**示例** (scenario_10754_no_control):
```
场景ID: scenario_10754_no_control
事件类型: 交通事故
管控策略: NO_CONTROL
模拟时间段: 2.5 小时（固定值）
```

**验证**:
- [ ] 场景ID 正确显示
- [ ] 事件类型 正确显示
- [ ] 管控策略 正确显示
- [ ] 模拟时间段显示固定值 2.5 小时

---

#### TC-2.3: 输出配置选项

**步骤**:
1. 打开创建模态框
2. 查看输出配置部分
3. 观察可用的复选框

**预期结果**:
- ✅ 只显示 2 个复选框：
  - ☑ EdgeData（路段流量和速度数据）
  - ☑ TripInfo（车辆行程信息）
- ✅ 两个复选框默认都勾选
- ✅ 用户可以修改这两个选项
- ✅ 不显示其他选项（Summary、VehRoute等）

**验证**:
- [ ] 只显示 2 个复选框
- [ ] 两个复选框都默认勾选
- [ ] 复选框可以被取消勾选
- [ ] 复选框可以被重新勾选

---

#### TC-2.4: 创建案例

**步骤**:
1. 打开创建模态框
2. 保持默认设置（EdgeData + TripInfo 都勾选）
3. 点击"✓ 创建"按钮
4. 观察响应

**预期结果**:
- ✅ 按钮变为禁用状态（显示"创建中..."）
- ✅ 后端处理请求（观察 Network 标签中的 POST 请求）
- ✅ 显示成功消息，包含新创建的案例ID
- ✅ 模态框自动关闭
- ✅ 场景表格刷新，显示新案例状态

**Network 请求验证**:
```
POST /api/v1/case/create-case-with-simulation
Request Body:
{
  "scenario_id": "scenario_10754_no_control",
  "event_id": "10754",
  "event_type": "01_accident",
  "strategy": "NO_CONTROL",
  "case_name": null,  // 后端自动生成
  "simulation_type": "microscopic",
  "random_seed": null,  // 后端自动生成
  "output_config": {
    "generate_edgedata": true,
    "generate_summary": true,
    "generate_tripinfo": true,
    "generate_vehroute": false
  },
  ...
}

Response:
{
  "code": 0,
  "message": "成功创建案例和仿真配置",
  "data": {
    "case_id": "case_xxxx_xxxx",
    "simulation_id": "sim_xxxx_xxxx",
    ...
  }
}
```

**验证**:
- [ ] POST 请求成功发送（状态码 200）
- [ ] 返回的 case_id 正确
- [ ] 成功消息显示在页面
- [ ] 模态框自动关闭

---

#### TC-2.5: 修改输出配置后创建

**步骤**:
1. 打开创建模态框
2. **取消勾选** EdgeData（只保留 TripInfo）
3. 点击"✓ 创建"按钮
4. 观察请求和响应

**预期结果**:
- ✅ 后端接收的 output_config 中：
  - generate_edgedata: false
  - generate_tripinfo: true
- ✅ 案例创建成功
- ✅ 显示新的案例ID

**Network 请求验证**:
```
"output_config": {
  "generate_edgedata": false,  // 用户修改的
  "generate_summary": true,
  "generate_tripinfo": true,
  "generate_vehroute": false
}
```

**验证**:
- [ ] EdgeData 复选框可以被取消
- [ ] TripInfo 复选框保持勾选
- [ ] 创建请求中的配置反映了用户的选择

---

#### TC-2.6: 不自动导航验证

**步骤**:
1. 打开创建模态框
2. 创建案例（点击"✓ 创建"）
3. 等待成功消息显示
4. 观察页面位置

**预期结果**:
- ✅ 成功消息显示
- ✅ **页面不会自动跳转**到其他页面（如 case-simulation-center.html）
- ✅ 用户仍然停留在场景浏览器页面
- ✅ 可以继续创建其他案例或执行其他操作

**验证**:
- [ ] 创建成功后页面 URL 保持不变
- [ ] 场景表格仍然可见
- [ ] 可以继续点击其他场景的按钮

---

#### TC-2.7: 取消创建

**步骤**:
1. 打开创建模态框
2. 点击"取消"按钮
3. 观察模态框

**预期结果**:
- ✅ 模态框立即关闭
- ✅ 没有创建任何案例
- ✅ 返回到场景表格
- ✅ 网络中没有 POST 请求

**验证**:
- [ ] 点击"取消"后模态框关闭
- [ ] Network 标签中没有看到 POST 请求
- [ ] 场景表格仍然显示原来的内容

---

### 测试集合 #3: 按钮布局和交互

#### TC-3.1: 按钮顺序和样式

**步骤**:
1. 打开场景浏览器
2. 查看场景表格中任意行的按钮

**预期结果**:
- ✅ 左边按钮：深蓝色，显示"📋 详情"
- ✅ 右边按钮：蓝色，显示"➕ 创建"
- ✅ 按钮之间有间隔
- ✅ 按钮大小适当

**验证**:
- [ ] 详情按钮在左边
- [ ] 创建按钮在右边
- [ ] 详情按钮背景色深蓝（#1976d2）
- [ ] 创建按钮背景色蓝色

---

#### TC-3.2: 按钮悬停效果

**步骤**:
1. 将鼠标移到"详情"按钮上
2. 观察按钮状态
3. 将鼠标移到"创建"按钮上
4. 观察按钮状态

**预期结果**:
- ✅ 悬停时按钮背景色变亮
- ✅ 悬停时按钮略微上移（transform: translateY(-1px)）
- ✅ 悬停时显示阴影效果
- ✅ 鼠标光标变为"手指"形状

**验证**:
- [ ] 详情按钮悬停时背景色变深蓝
- [ ] 创建按钮悬停时背景色变蓝
- [ ] 悬停时有阴影效果
- [ ] 鼠标光标变为指针

---

### 测试集合 #4: 数据回退机制

#### TC-4.1: event_description.json 不存在时的回退

**先决条件**:
- 找到一个 event_description.json 不存在的场景
- 或临时重命名某个 event_description.json 文件

**步骤**:
1. 打开不存在 event_description.json 的场景的详情
2. 观察 Network 标签
3. 检查字段填充情况

**预期结果**:
- ✅ Network 标签显示 event_description.json 返回 404
- ✅ 没有显示错误提示或异常
- ✅ 所有字段仍然被填充（使用 scenario_index.json 的数据）
- ✅ 用户无感知回退过程

**验证**:
- [ ] event_description.json 请求返回 404
- [ ] 模态框正常显示所有字段
- [ ] 没有显示错误或警告信息
- [ ] Console 中可能有 warn 日志，但不影响功能

---

#### TC-4.2: 网络错误时的回退

**先决条件**:
- 打开浏览器开发者工具
- Network 标签 → 右键 → Offline 模式

**步骤**:
1. 启用离线模式
2. 打开任意场景的详情
3. 观察模态框

**预期结果**:
- ✅ event_description.json 请求失败（Network Error）
- ✅ 没有显示错误提示
- ✅ 所有字段仍然被填充
- ✅ 用户无感知（自动降级）

**验证**:
- [ ] 离线模式下模态框仍能显示
- [ ] 所有字段有值（不是"未知"）
- [ ] 没有显示错误提示

---

#### TC-4.3: 部分字段缺失时的处理

**步骤**:
1. 打开详情模态框
2. 检查所有字段是否都有值
3. 如果某个字段显示"未知"，检查是否正常

**预期结果**:
- ✅ 所有有数据的字段正常显示
- ✅ 缺少数据的字段显示"未知"（不是空白）
- ✅ 没有显示错误或异常

**验证**:
- [ ] 字段要么有值，要么显示"未知"
- [ ] 没有空白字段
- [ ] 没有 undefined 或 null 显示

---

### 测试集合 #5: 跨浏览器兼容性

#### TC-5.1: Chrome 浏览器

**步骤**:
1. 在 Google Chrome 中打开场景浏览器
2. 执行所有主要功能测试（TC-1.1 至 TC-4.3）

**预期结果**:
- ✅ 所有功能正常工作
- ✅ 没有控制台错误（红色）

**验证**:
- [ ] 详情模态框正常显示
- [ ] 创建模态框正常显示
- [ ] 网络请求成功
- [ ] 没有 JavaScript 错误

---

#### TC-5.2: Firefox 浏览器

**步骤**:
1. 在 Mozilla Firefox 中打开场景浏览器
2. 执行关键功能测试（TC-1.1, TC-1.2, TC-2.1, TC-2.4）

**预期结果**:
- ✅ 模态框显示
- ✅ 数据加载
- ✅ 创建案例

**验证**:
- [ ] 详情模态框显示正确
- [ ] 创建模态框显示正确
- [ ] 没有浏览器兼容性问题

---

#### TC-5.3: Edge 浏览器

**步骤**:
1. 在 Microsoft Edge 中打开场景浏览器
2. 执行关键功能测试

**预期结果**:
- ✅ 所有功能正常工作
- ✅ 样式正确显示

**验证**:
- [ ] 页面加载正确
- [ ] 模态框显示正确
- [ ] 按钮交互正常

---

## 📊 测试执行记录

### 测试矩阵

| 测试编号 | 用例名称 | 状态 | 备注 |
|---------|---------|------|------|
| TC-1.1 | 打开详情模态框 | [ ] | |
| TC-1.2 | 事件描述加载 | [ ] | |
| TC-1.3 | 位置信息填充 | [ ] | |
| TC-1.4 | 时间信息填充 | [ ] | |
| TC-1.5 | 事件影响填充 | [ ] | |
| TC-1.6 | 关闭详情模态框 | [ ] | |
| TC-2.1 | 打开创建模态框 | [ ] | |
| TC-2.2 | 预填信息验证 | [ ] | |
| TC-2.3 | 输出配置选项 | [ ] | |
| TC-2.4 | 创建案例 | [ ] | |
| TC-2.5 | 修改配置后创建 | [ ] | |
| TC-2.6 | 不自动导航验证 | [ ] | |
| TC-2.7 | 取消创建 | [ ] | |
| TC-3.1 | 按钮顺序和样式 | [ ] | |
| TC-3.2 | 按钮悬停效果 | [ ] | |
| TC-4.1 | 缺失文件回退 | [ ] | |
| TC-4.2 | 网络错误回退 | [ ] | |
| TC-4.3 | 字段缺失处理 | [ ] | |
| TC-5.1 | Chrome 兼容性 | [ ] | |
| TC-5.2 | Firefox 兼容性 | [ ] | |
| TC-5.3 | Edge 兼容性 | [ ] | |

---

## 🔍 错误排查指南

### 问题 #1: event_description.json 404 错误

**症状**: Network 标签显示 event_description.json 返回 404

**可能原因**:
1. 文件确实不存在 → 正常（自动回退）
2. 路径映射错误 → 检查 mapEventTypeToFolder() 函数

**排查步骤**:
```bash
# 检查文件是否存在
ls output/scenarios/01_accident/scenario_10754_no_control/event_description.json

# 检查 JavaScript 日志
# 打开 Console 标签，查看是否有 warn 消息
```

**解决方案**:
- 如果文件不存在 → 检查是否生成了这个文件
- 如果路径错误 → 检查 mapEventTypeToFolder() 的映射是否正确

---

### 问题 #2: 模态框不显示

**症状**: 点击"详情"或"创建"按钮，没有显示模态框

**可能原因**:
1. JavaScript 有语法错误
2. HTML 中缺少模态框定义
3. CSS 中模态框被隐藏了

**排查步骤**:
```bash
# 检查 JavaScript 语法
node -c frontend/scenarios/scenario_browser.js

# 检查浏览器 Console 是否有错误信息
# F12 → Console 标签
```

**解决方案**:
- 检查 Console 中的错误信息
- 在 Elements 标签中搜索 "scenarioDetailsModal" 或 "caseCreationModal"
- 确保 CSS 文件正确加载

---

### 问题 #3: 字段显示"未知"

**症状**: 模态框中的某些字段显示"未知"

**可能原因**:
1. event_description.json 中不存在该字段
2. scenario_index.json 中也不存在该字段
3. 字段名拼写错误

**排查步骤**:
```bash
# 检查 event_description.json 内容
curl http://localhost:8000/output/scenarios/01_accident/scenario_10754_no_control/event_description.json

# 检查 scenario_index.json 中的对应字段
curl http://localhost:8000/output/scenarios/scenario_index.json | jq '.scenarios[0]'
```

**解决方案**:
- 确认源数据中确实包含该字段
- 检查 JavaScript 代码中的字段名是否拼写正确

---

### 问题 #4: 创建案例失败

**症状**: 点击"✓ 创建"后显示错误信息

**可能原因**:
1. 后端 API 返回错误
2. 请求参数格式不正确
3. 后端服务未启动

**排查步骤**:
```bash
# 检查后端 API 是否运行
curl http://localhost:8000/api/v1/health

# 检查 Network 标签中的 POST 请求
# 查看 Response 中的错误信息
```

**解决方案**:
- 确保后端 API 正常运行（./start_api.ps1）
- 检查 Network 标签中的错误响应内容
- 查看后端日志获取详细信息

---

### 问题 #5: 模态框样式不正确

**症状**: 模态框显示但样式错乱（颜色、大小、位置不对）

**可能原因**:
1. CSS 文件未加载
2. CSS 被其他样式覆盖
3. 浏览器缓存问题

**排查步骤**:
```bash
# 硬刷新浏览器（清除缓存）
# Windows: Ctrl + Shift + Delete
# Mac: Cmd + Shift + Delete

# 在 Elements 标签中检查应用的样式
# 查看 Styles 面板中的 CSS 规则
```

**解决方案**:
- 清除浏览器缓存并重新加载页面
- 检查 scenario_browser.css 是否正确加载（Network 标签）

---

## ✅ 测试完成清单

### 所有测试完成后

- [ ] 所有 21 个测试用例都通过
- [ ] 没有控制台错误（红色）
- [ ] 在 Chrome、Firefox、Edge 中都测试过
- [ ] 网络请求正常（没有 5xx 错误）
- [ ] event_description.json 正确加载（或正确回退）
- [ ] 按钮交互正常
- [ ] 模态框显示正确
- [ ] 工作流符合预期

### 签名

- 测试负责人: _______________
- 测试日期: _______________
- 测试结果: ✅ 通过 / ❌ 失败

---

## 📞 支持和反馈

如遇到任何问题：

1. **检查 Console**: F12 → Console 标签，查看是否有错误
2. **检查 Network**: F12 → Network 标签，查看网络请求状态
3. **查看源代码**: 相关文档和代码都在项目根目录和 frontend/scenarios/ 目录中
4. **查看日志**: 后端日志可能包含更多信息

---

**Status**: 📋 **READY FOR TESTING**

**Last Updated**: 2025-11-14
**Version**: 1.0

