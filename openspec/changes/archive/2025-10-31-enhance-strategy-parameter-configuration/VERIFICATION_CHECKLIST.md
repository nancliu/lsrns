# Phase 1-2 验证清单

**目的**: 验证 Phase 1-2 功能正确集成并按预期工作
**执行人**: ___________
**执行日期**: ___________

---

## 🚀 启动准备

### 1. 启动后端服务器

```powershell
cd D:\projects\OD_SIM
.\start_api.bat
# 或
python api\main.py
```

**验证点**:
- [ ] 服务器启动成功 (无错误)
- [ ] 访问 http://localhost:8000 显示首页
- [ ] 访问 http://localhost:8000/docs 显示 API 文档
- [ ] API 文档中存在 `POST /api/v1/control/edges/batch-info` 端点

---

## 📝 Phase 1: 智能参数输入验证

### Task 1.1: 智能占位符生成

**测试步骤**:
1. 访问 http://localhost:8000/control/templates.html
2. 选择 **DHS (动态硬路肩)** 模板
3. 点击 [下一步] 进入步骤 2
4. 随意选择几个路段 (至少选择3个，车道数≥4的路段)
5. 点击 [下一步] 进入步骤 3
6. 查看 `dhs_interval_array` 参数的占位符文本

**预期结果**:
- [ ] 占位符显示时间段格式示例:
  ```
  时段格式示例:
  [
    [7, 9],
    [17, 19]
  ]

  每行一个时间段 [开始小时, 结束小时]
  ```

**测试其他参数**:
- [ ] VSS 模板: `speed_limits` 参数显示限速值示例
- [ ] TEC 模板: `vehicle_types` 参数显示车型列表示例

---

### Task 1.2: 数值范围验证

**测试步骤**:
1. 在 VSS 模板的步骤 3
2. 找到 `min_speed` 参数 (假设范围: 30-130)
3. 输入无效值: `200`
4. 点击输入框外部 (触发 blur 事件)

**预期结果**:
- [ ] 显示范围提示: "范围: 30-130 | 单位: km/h"
- [ ] 显示单位标签: "km/h"
- [ ] 显示错误消息: "值不能大于 130" (红色背景)

**测试有效值**:
5. 输入有效值: `80`
6. 点击输入框外部

**预期结果**:
- [ ] 显示成功消息: "✓ 有效" (绿色背景)

---

### Task 1.3: 数组参数格式验证

#### 测试 JSON 格式

**测试步骤**:
1. 在 DHS 模板的 `dhs_interval_array` 参数
2. 输入有效 JSON:
   ```json
   [[7, 9], [17, 19]]
   ```
3. 点击输入框外部

**预期结果**:
- [ ] 显示成功消息: "✓ 已识别 2 个项目" (绿色背景)

#### 测试换行格式

**测试步骤**:
4. 清空输入框，输入换行格式:
   ```
   [7, 9]
   [17, 19]
   ```
5. 点击输入框外部

**预期结果**:
- [ ] 显示成功消息: "✓ 已识别 2 个项目"

#### 测试无效时间

**测试步骤**:
6. 输入无效时间:
   ```json
   [[7, 9], [25, 27]]
   ```
7. 点击输入框外部

**预期结果**:
- [ ] 显示错误消息: "时段2无效: 开始小时25超出范围(0-24)" (红色背景)

#### 测试跨午夜警告

**测试步骤**:
8. 输入跨午夜时段:
   ```json
   [[22, 2]]
   ```
9. 点击输入框外部

**预期结果**:
- [ ] 显示警告消息: "⚠️ 时段1跨越午夜(22-2)，请确认是否正确" (黄色背景)

---

### Task 1.4: Enum 下拉选择器

**测试步骤**:
1. 找到任意 enum 类型参数 (如 `control_mode`)
2. 查看下拉选择器

**预期结果**:
- [ ] 显示下拉选择器 (不是文本输入框)
- [ ] 如果非必填，第一项为 "-- Select --"
- [ ] 枚举值列表正确显示
- [ ] 默认值自动选中

---

## 📊 Phase 2: 路段展示增强验证

### Task 2.1: 路段信息表组件

**测试步骤**:
1. 选择 DHS 模板
2. 在步骤 2 选择至少 5 个连续路段 (G4202 路线，车道数≥4)
   - 建议选择: -5880, -5881, -5882, -5883, -5884
3. 点击 [下一步] 进入步骤 3

**预期结果**:
- [ ] 显示路段信息表格
- [ ] 表格包含 10 列: 序号、Edge ID、路线、路段、起始桩号、结束桩号、长度、车道数、方向、节点类型
- [ ] 桩号格式化为 "K10+200" 格式
- [ ] 方向翻译为中文 (上行/下行/顺时针/逆时针)
- [ ] 按起始桩号排序
- [ ] Edge ID 显示为蓝色等宽字体

---

### Task 2.2: 路段摘要统计

**预期结果**:
- [ ] 显示路段摘要卡片
- [ ] 摘要包含:
  - [ ] 已选择路段数: 5
  - [ ] 总长度: X.XX km
  - [ ] 覆盖路线: G4202
  - [ ] 车道数范围: 4-6 (或实际范围)
- [ ] 使用 Grid 布局，响应式显示

---

### Task 2.3: DHS 路段连续性验证

#### 测试连续路段 (无警告)

**测试步骤**:
1. 选择连续路段 (如 -5880 到 -5884)
2. 进入步骤 3

**预期结果**:
- [ ] 无警告横幅显示
- [ ] 表格正常显示

#### 测试不连续路段 (显示警告)

**测试步骤**:
3. 返回步骤 2
4. 选择不连续路段 (如 -5880, -5881, -5900)
   - 确保 -5881 和 -5900 之间存在 >50m 间隙
5. 进入步骤 3

**预期结果**:
- [ ] 显示黄色警告横幅:
  - [ ] 图标: ⚠️
  - [ ] 消息: "警告：所选路段不连续，DHS策略可能效果降低"
  - [ ] 详情: 显示具体间隙信息 (如 "K10+800 到 K11+200 之间存在 400m 间隙")
  - [ ] 提示: "如需调整路段选择，请返回第2步"
- [ ] 保存按钮仍然可用 (警告不阻止保存)

---

### Task 2.4: DHS 车道数验证

#### 测试车道数≥4 (无错误)

**测试步骤**:
1. 选择车道数≥4的路段
2. 进入步骤 3

**预期结果**:
- [ ] 无错误横幅显示
- [ ] 保存按钮可用

#### 测试车道数<4 (显示错误)

**测试步骤**:
3. 返回步骤 2
4. 选择至少一个车道数<4的路段
5. 进入步骤 3

**预期结果**:
- [ ] 显示红色错误横幅:
  - [ ] 图标: ❌
  - [ ] 消息: "DHS策略要求车道数≥4，以下路段不符合:"
  - [ ] 详情: 列出不符合的路段 (如 "-5900 (2车道)")
  - [ ] 提示: "请返回第2步重新选择符合要求的路段"
- [ ] **保存按钮被禁用**
- [ ] 鼠标悬停保存按钮显示提示: "存在验证错误，请返回第2步修正路段选择"

---

## 🔗 前端集成验证

### Task INT.1: 模板页面集成

**测试步骤**:
1. 打开浏览器开发者工具 (F12)
2. 访问 http://localhost:8000/control/templates.html
3. 查看 Console 标签

**预期结果**:
- [ ] 无 JavaScript 错误
- [ ] `parameter_form.js` 加载成功
- [ ] `edge_display.js` 加载成功
- [ ] 无 404 错误 (js 文件路径正确)

**测试步骤**:
4. 选择模板并进入步骤 3
5. 查看 Console 输出

**预期结果**:
- [ ] 看到 `[initializeEdgeDisplay]` 相关日志
- [ ] 看到 EdgeDisplayTable 实例化成功
- [ ] 无 "容器元素未找到" 错误
- [ ] 无 "EdgeDisplayTable 类未加载" 错误

---

### Task INT.2: 后端 API 端点

#### 测试 API 端点存在

**测试步骤**:
1. 访问 http://localhost:8000/docs
2. 搜索 "batch-info"

**预期结果**:
- [ ] 找到 `POST /api/v1/control/edges/batch-info` 端点
- [ ] 端点描述为 "批量获取路段详细信息"
- [ ] Request body 包含 `edge_ids: array[string]`
- [ ] Response 为 `array[object]`

#### 测试 API 功能

**测试步骤**:
3. 点击 "Try it out"
4. 输入测试数据:
   ```json
   {
     "edge_ids": ["-5880", "-5881", "-5882"]
   }
   ```
5. 点击 "Execute"

**预期结果**:
- [ ] 状态码: 200
- [ ] 返回包含 3 个路段信息的数组
- [ ] 每个对象包含字段:
  - [ ] edge_id (字符串)
  - [ ] route_code (字符串)
  - [ ] section_code (字符串)
  - [ ] start_stake (数值，单位:米)
  - [ ] end_stake (数值，单位:米)
  - [ ] length_m (数值，单位:米) ← 注意是 length_m 不是 length
  - [ ] lane_count (整数) ← 注意是 lane_count 不是 num_lanes
  - [ ] direction (字符串) ← 注意是 direction 不是 route_direction
  - [ ] node_type (字符串)
  - [ ] gantry_count (整数)

#### 测试 Network 请求

**测试步骤**:
6. 回到前端页面，打开 Network 标签
7. 选择模板并进入步骤 3
8. 查看 Network 请求

**预期结果**:
- [ ] 看到 `POST /api/v1/control/edges/batch-info` 请求
- [ ] 请求体包含选中的 edge_ids
- [ ] 响应状态码: 200
- [ ] 响应内容包含完整路段信息

---

## 🎨 样式验证

### CSS 样式检查

**测试步骤**:
1. 在步骤 3 页面，使用浏览器开发者工具检查元素

**预期结果**:
- [ ] 数值输入框有 `.number-input-wrapper` 容器
- [ ] 单位标签有 `.unit-label` 类
- [ ] 参数提示有 `.parameter-hint` 类
- [ ] 验证反馈有 `.parameter-feedback` 类
- [ ] 路段摘要有 `.edge-summary` 类
- [ ] 路段表格有 `.edge-table` 类
- [ ] 验证错误有 `.validation-error` 类
- [ ] 验证警告有 `.validation-warning` 类

**视觉检查**:
- [ ] 错误消息: 红色背景，红色左边框
- [ ] 警告消息: 黄色背景，橙色左边框
- [ ] 成功消息: 绿色背景，绿色左边框
- [ ] 表格标题: 蓝色背景，白色文字
- [ ] 表格行悬停: 浅灰色背景

---

## 🐛 错误处理验证

### 无路段选择

**测试步骤**:
1. 选择模板，不选择任何路段
2. 直接点击 [下一步] 进入步骤 3

**预期结果**:
- [ ] 显示空状态提示:
  - "❌ 未选择路段"
  - "请返回第2步选择路段"
- [ ] 保存按钮被禁用
- [ ] 鼠标悬停提示: "请先返回第2步选择路段"

---

### API 错误处理

**测试步骤**:
1. 停止后端服务器
2. 在前端选择路段并进入步骤 3

**预期结果**:
- [ ] 显示错误提示 (不崩溃)
- [ ] Console 中有错误日志
- [ ] 用户可以返回上一步

---

## ✅ 最终验证

### 完整工作流测试

**测试步骤**:
1. 选择 **DHS (动态硬路肩)** 模板
2. 选择 5 个连续、车道数≥4 的路段 (如 G4202: -5880 到 -5884)
3. 进入步骤 3
4. 验证路段表格显示正确
5. 验证路段摘要统计正确
6. 验证无验证错误/警告
7. 填写所有必填参数:
   - `dhs_interval_array`: `[[7, 9], [17, 19]]`
   - 其他参数使用默认值
8. 点击 [生成策略实例]

**预期结果**:
- [ ] 所有步骤无错误
- [ ] 路段信息完整显示
- [ ] 参数验证通过
- [ ] 策略实例生成成功 (后续功能)

---

## 📝 验证结果

### 总体评分

- [ ] ✅ 全部通过
- [ ] ⚠️ 部分通过 (请列出问题)
- [ ] ❌ 未通过 (请列出严重问题)

### 发现的问题

| 编号 | 问题描述 | 严重性 | 截图/日志 |
|------|---------|--------|----------|
| 1.   |         | 高/中/低 |          |
| 2.   |         | 高/中/低 |          |
| 3.   |         | 高/中/低 |          |

### 备注

```
请在此处添加任何额外的观察、建议或反馈。
```

---

**验证完成日期**: ___________
**验证人签名**: ___________
**状态**: [ ] 通过验收 / [ ] 需要修正
