# 手动测试清单：时间选择器可视化

**OpenSpec 变更**: `add-streamlined-time-selector-visualization`
**测试日期**: 2025-10-30
**测试环境**: Windows 10/11, Chrome/Edge/Firefox 最新版本

## 前置条件

1. ✅ API 服务器已启动：
   ```bash
   .\start_api.ps1
   # 或
   python api\main.py
   ```

2. ✅ 访问测试页面：
   ```
   http://localhost:8000/control/templates.html
   ```

## 测试类别 1：基础渲染（VSS 策略）

### 测试 1.1：时间轴渲染位置

**步骤**：
1. 打开 `http://localhost:8000/control/templates.html`
2. 在策略模板列表中选择任意 VSS 模板（卡片标题包含"可变限速"）
3. 点击"下一步：配置参数"按钮

**预期结果**：
- [ ] 时间轴组件出现在表格**上方**
- [ ] 时间轴包含在统一的白色卡片容器中
- [ ] 卡片有圆角、边框和阴影
- [ ] 无控制台错误

**实际结果**：
```
（在此填写测试结果）
```

---

### 测试 1.2：24 小时标记

**步骤**：
1. 继续上述测试场景
2. 检查时间轴顶部的小时标记

**预期结果**：
- [ ] 显示 0-23 或 00-23 的小时标记
- [ ] 标记均匀分布在时间轴宽度上
- [ ] 字体大小适中，易于阅读

**实际结果**：
```
（在此填写测试结果）
```

---

### 测试 1.3：时间槽颜色编码

**步骤**：
1. 继续上述测试场景
2. 观察时间轴中间的彩色时间槽

**预期结果**：
- [ ] 至少显示 1 个彩色时间槽
- [ ] 不同速度值显示不同颜色：
  - >= 100 km/h：绿色
  - 80-99 km/h：蓝色
  - 60-79 km/h：橙色
  - < 60 km/h：红色
- [ ] 时间槽内显示速度值标签（如 "100 km/h"）
- [ ] 时间槽宽度与时间段长度成正比

**实际结果**：
```
（在此填写测试结果）
```

---

## 测试类别 2：实时同步

### 测试 2.1：表格变化更新时间轴

**步骤**：
1. 在参数配置页面，定位到表格编辑器
2. 修改第一行的速度值：
   - 原值（如 100）→ 修改为 50
3. 点击输入框外部或按 Tab 键失去焦点
4. 等待 500ms

**预期结果**：
- [ ] 时间轴中对应的时间槽颜色**立即**变化（从绿色变为红色）
- [ ] 标签文本更新为新速度值
- [ ] 无控制台错误
- [ ] 更新动画平滑（CSS 过渡）

**实际结果**：
```
（在此填写测试结果）
```

---

### 测试 2.2：快速连续修改（防抖测试）

**步骤**：
1. 快速连续修改多个表格行的速度值（<300ms 间隔）
2. 观察时间轴更新频率

**预期结果**：
- [ ] 时间轴**不会**每次修改都立即更新
- [ ] 等待 300ms 后统一更新（防抖生效）
- [ ] 最终时间轴状态与表格数据一致

**实际结果**：
```
（在此填写测试结果）
```

---

## 测试类别 3：可选参数处理

### 测试 3.1：VSS 天气响应式模板

**步骤**：
1. 选择模板："可变限速 - 天气响应式"（vss_weather_based）
2. 点击"选择管控路段"按钮
3. 选择至少 1 条路段，点击"确认选择"
4. 在参数配置表单中：
   - 填写策略名称：`测试_可选参数_天气`
   - **不填写** `weather_condition`（可选参数）
   - **不填写** `visibility_distance`（可选参数）
   - 保留默认的 `speed_steps` 值（必填参数）
5. 点击"创建策略实例"

**预期结果**：
- [ ] **成功创建策略实例**（无验证错误）
- [ ] 控制台显示日志：`Skipping optional parameter with empty value: weather_condition`
- [ ] 控制台显示日志：`Skipping optional parameter with empty value: visibility_distance`
- [ ] 显示成功提示消息

**实际结果**：
```
（在此填写测试结果）
```

---

### 测试 3.2：填写可选参数

**步骤**：
1. 重复上述步骤，但这次填写可选参数：
   - `weather_condition`：选择 "雨天"
   - `visibility_distance`：输入 200
2. 点击"创建策略实例"

**预期结果**：
- [ ] 成功创建策略实例
- [ ] 可选参数被包含在请求中
- [ ] 无控制台错误

**实际结果**：
```
（在此填写测试结果）
```

---

## 测试类别 4：UI 样式和布局

### 测试 4.1：统一卡片式布局

**步骤**：
1. 选择任意 VSS 模板并进入参数配置页面
2. 检查时间轴组件的视觉样式

**预期结果**：
- [ ] 时间轴和表格都在同一个白色卡片容器中
- [ ] 卡片有以下样式：
  - 白色背景
  - 灰色边框（1px solid #e5e7eb）
  - 圆角（8px border-radius）
  - 轻微阴影（box-shadow）
  - 内边距（20px padding）
- [ ] 时间轴与表格之间有适当间距（16px）

**实际结果**：
```
（在此填写测试结果）
```

---

### 测试 4.2：描述文本和使用提示

**步骤**：
1. 继续上述测试场景
2. 检查时间轴组件附近的文本

**预期结果**：
- [ ] 时间轴上方显示描述文本（如"时间-限速值序列，支持3-5个步骤"）
- [ ] 表格上方或下方显示使用提示（如"使用表格编辑器配置速度步骤"）
- [ ] 文本样式清晰易读（灰色文字，小字号）

**实际结果**：
```
（在此填写测试结果）
```

---

### 测试 4.3：响应式布局

**步骤**：
1. 调整浏览器窗口宽度：
   - 1920px（全屏）
   - 1366px（笔记本常见分辨率）
   - 1024px（较小屏幕）
2. 观察时间轴布局变化

**预期结果**：
- [ ] 时间轴宽度自适应容器宽度
- [ ] 小时标记和时间槽保持正确比例
- [ ] 标签文本不溢出或重叠
- [ ] 所有分辨率下布局不破坏

**实际结果**：
```
（在此填写测试结果）
```

---

## 测试类别 5：错误处理和边界情况

### 测试 5.1：极端速度值

**步骤**：
1. 在表格中输入以下速度值：
   - 0 km/h
   - 200 km/h
   - -10 km/h（无效值）
2. 观察时间轴反应

**预期结果**：
- [ ] 0 km/h：显示红色
- [ ] 200 km/h：显示绿色
- [ ] 无效值：时间轴优雅处理（显示默认颜色或跳过）
- [ ] 无 JavaScript 错误

**实际结果**：
```
（在此填写测试结果）
```

---

### 测试 5.2：空表格数据

**步骤**：
1. 删除表格中所有行（如果允许）
2. 或选择一个 default_value 为空数组的模板
3. 观察时间轴渲染

**预期结果**：
- [ ] 时间轴显示空状态或默认视图
- [ ] 不崩溃或显示错误
- [ ] 控制台有友好的警告日志

**实际结果**：
```
（在此填写测试结果）
```

---

## 测试类别 6：跨浏览器兼容性

### 测试 6.1：Chrome

**浏览器版本**：Chrome ____

**测试结果**：
- [ ] 所有功能正常
- [ ] 样式渲染正确
- [ ] 无控制台错误

**问题记录**：
```
（如有问题，在此记录）
```

---

### 测试 6.2：Edge

**浏览器版本**：Edge ____

**测试结果**：
- [ ] 所有功能正常
- [ ] 样式渲染正确
- [ ] 无控制台错误

**问题记录**：
```
（如有问题，在此记录）
```

---

### 测试 6.3：Firefox

**浏览器版本**：Firefox ____

**测试结果**：
- [ ] 所有功能正常
- [ ] 样式渲染正确
- [ ] 无控制台错误

**问题记录**：
```
（如有问题，在此记录）
```

---

## 回归测试

### 测试 7.1：现有表格编辑功能

**步骤**：
1. 选择 VSS 模板
2. 使用表格编辑器添加/删除/修改行
3. 验证表格功能未受影响

**预期结果**：
- [ ] 添加行：正常工作
- [ ] 删除行：正常工作
- [ ] 编辑单元格：正常工作
- [ ] 数据验证：正常工作

**实际结果**：
```
（在此填写测试结果）
```

---

### 测试 7.2：策略实例创建流程

**步骤**：
1. 完整走通策略创建流程：
   - 选择模板
   - 选择路段
   - 配置参数
   - 创建实例
2. 验证策略实例正确保存

**预期结果**：
- [ ] 完整流程无阻塞
- [ ] 策略实例成功创建
- [ ] 参数正确保存（包括 speed_steps）
- [ ] 可在策略列表中查看新创建的实例

**实际结果**：
```
（在此填写测试结果）
```

---

## 测试总结

### 通过的测试数量

- **基础渲染**: ____ / 3
- **实时同步**: ____ / 2
- **可选参数**: ____ / 2
- **UI 样式**: ____ / 3
- **错误处理**: ____ / 2
- **跨浏览器**: ____ / 3
- **回归测试**: ____ / 2

**总计**: ____ / 17 测试通过

### 发现的问题

1.
2.
3.

### 建议改进

1.
2.
3.

### 测试结论

- [ ] **所有 P0 测试通过** - 可以标记为完成
- [ ] **存在阻塞性问题** - 需要修复
- [ ] **存在非阻塞性问题** - 可延后修复

---

**测试人员签名**: ________________
**测试完成日期**: ________________
